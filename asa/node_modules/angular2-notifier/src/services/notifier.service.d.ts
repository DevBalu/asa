import { ApplicationRef, ComponentResolver } from '@angular/core';
export declare class NotifierService {
    private componentResolver;
    private notifierContainer;
    constructor(applicationRef: ApplicationRef, componentResolver: ComponentResolver);
    notify(type: string, message: string): Promise<any>;
    default(message: string): Promise<any>;
    info(message: string): Promise<any>;
    success(message: string): Promise<any>;
    warning(message: string): Promise<any>;
    error(message: string): Promise<any>;
    clearAll(): Promise<any>;
    clearOldest(): Promise<any>;
    clearNewest(): Promise<any>;
    private setupComponent(rootComponent);
}
