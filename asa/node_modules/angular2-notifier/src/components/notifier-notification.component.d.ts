import { AfterViewInit, ElementRef, Renderer } from '@angular/core';
import { NotifierGlobalConfig } from './../models/notifier-global-config.model';
import { NotifierAnimationService } from './../services/notifier-animations.service';
import { NotifierTimerService } from './../services/notifier-timer.service';
export declare class NotifierNotificationComponent implements AfterViewInit {
    private notification;
    private created;
    private dismiss;
    private renderer;
    private notifierAnimationService;
    private notifierTimerService;
    private config;
    private element;
    private currentHeight;
    private currentShift;
    constructor(elementRef: ElementRef, renderer: Renderer, notifierAnimationService: NotifierAnimationService, notifierTimerService: NotifierTimerService, notifierGlobalConfig: NotifierGlobalConfig);
    ngAfterViewInit(): void;
    getHeight(): number;
    getConfig(): NotifierGlobalConfig;
    getCurrentHeight(): number;
    getCurrentShift(): number;
    show(): Promise<any>;
    hide(): Promise<any>;
    shift(value: number, toMakePlace: boolean): Promise<any>;
    private setup();
    private runTimer();
    private pauseTimer();
    private stopTimer();
    private onDismiss();
    private onMouseover();
    private onMouseout();
    private onClick();
}
