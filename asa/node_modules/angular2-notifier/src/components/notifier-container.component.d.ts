import { NotifierGlobalConfig } from './../models/notifier-global-config.model';
import { NotifierAction } from './../models/notifier-action.model';
export declare class NotifierContainerComponent {
    private config;
    private notifications;
    private queue;
    private tempNotificationResolver;
    constructor(notifierGlobalConfig: NotifierGlobalConfig);
    doAction(action: NotifierAction): Promise<any>;
    private doNextActionInQueue();
    private addNotification(notification);
    private removeAllNotifications();
    private removeNotification(notificationComponent);
    private animateOutNotification(notificationComponent);
    private animateShiftNotifications(notifications, value, toMakePlace);
    private getNotificationIndex(notificationComponent);
    private onCreated(notificationComponent);
    private onDismiss(notificationComponent);
}
